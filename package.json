{
  "name": "servio-challenge",
  "private": true,
  "version": "1.0.0",
  "description": "Service catalog + service detail challenge: Angular 20 front-end and a Node/TypeScript mock API",
  "scripts": {
    "setup": "npm --prefix api install && npm --prefix web install",
    "api": "npm --prefix api run dev",
    "web": "npm --prefix web start",
    "build": "npm --prefix web run build",
    "typecheck": "npm --prefix api run typecheck && npm --prefix web run typecheck"
  }
}
